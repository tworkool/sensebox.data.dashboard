# Mock Data

1. Download Mockoon
2. Import `mockoon_data.json` (which is an OpenAPI v3 json specification file) under `Import/export`
3. Run the Mockoon mock server under `localhost:3001` (don't forget to set the `MOCK_API_DATA` flag to `True` to enable mocking)
